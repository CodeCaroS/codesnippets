## 2026-07-11 - Repository wrapper timeout

- **Context:** Pre-commit status inspection.
- **Observed:** `rtk git status --short` timed out after 12 seconds without output.
- **Expected:** Repository status returned promptly.
- **Cause:** Unknown
- **Fix:** Used direct Git for diagnosis.
- **Prevention:** Investigate the wrapper if this recurs.
- **Status:** Mitigated

## 2026-07-11 - Glob search syntax mismatch

- **Context:** Initial repository scan for duplicated TypeScript files.
- **Observed:** `rtk find`/`rg` glob arguments were interpreted incorrectly on Windows and returned errors.
- **Expected:** File list returned on the first scan.
- **Cause:** Wrapper-specific glob syntax was assumed instead of verified.
- **Fix:** Used `rtk proxy rg --files -g '*.ts' -g '*.tsx'`.
- **Prevention:** Prefer the verified `rtk proxy rg --files -g` form for Windows glob scans.
- **Status:** Mitigated

## 2026-07-11 - RTK git inspection timeout recurred

- **Context:** Final diff and status verification.
- **Observed:** `rtk git diff --stat; rtk git diff --check; rtk git status --short` timed out again without output.
- **Expected:** Git inspection completed promptly.
- **Cause:** Unknown; same wrapper symptom as the existing repository wrapper timeout entry.
- **Fix:** Used direct Git read-only commands for verification.
- **Prevention:** Investigate or bypass the RTK Git wrapper for repository inspection until its timeout cause is known.
- **Status:** Open

## 2026-07-11 - Storage helper refactor typo

- **Context:** Replacing duplicated browser localStorage access.
- **Observed:** Client template-store test returned no persisted templates; full client test run had dependent routing failures.
- **Expected:** Valid persisted templates loaded unchanged.
- **Cause:** Refactor callback referenced undefined `parsedValue` instead of its `value` argument.
- **Fix:** Corrected callback variable and reran targeted verification.
- **Prevention:** Run affected unit test immediately after mechanical refactor before full suite.
- **Status:** Mitigated

## 2026-07-11 - Push rejected by remote divergence

- **Context:** Pushing commit `d03c9f5` to `main`.
- **Observed:** Remote rejected the push because `main` contains commits not present locally.
- **Expected:** Push completed.
- **Cause:** Remote branch advanced independently.
- **Fix:** Rebase local commit onto remote `main` before retrying.
- **Prevention:** Check remote divergence before pushing when collaboration is active.
- **Status:** Mitigated
